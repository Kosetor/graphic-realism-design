# Tokens

| File | Use |
|------|-----|
| `tokens.css` | Drop into web / Electron / agent UI |
| `tokens.json` | Design tools, codegen, non-CSS runtimes |

Prefix: `--mgr-*` (MGR = Graphic Realism system id).

Keep CSS and JSON in sync. Breaking changes → CHANGELOG + SKILL version bump.
